table {
  display: table;
  width: 100%;
}

.pro-table {
  input {
    padding: 0.6em 1em;
    border-radius: 2rem;
    border: 1px solid #dfe2e5;
    outline: none;

    &:focus {
      border-color: #3eaf7c;
    }
  }
}